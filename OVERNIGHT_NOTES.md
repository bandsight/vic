# Overnight notes — 2026-04-19

## What I built

V2 of the workbench: the assistant-driven extraction app we talked about. Scope is **pay_tables end-to-end**. Other four sections are stubbed with status-only controls.

**Server is running on port 8765** (background process). Check/restart:
```bash
ps -ef | grep uvicorn         # should see one on :8765
# If not running:
cd /home/john/.openclaw/workspace/projects/eba-workbench
set -a; . /home/john/.openclaw/.env; set +a
.venv/bin/uvicorn main:app --port 8765
# then http://localhost:8765
```

## What I tested (against Pyrenees, ae521669, 204 pages)

1. **Smoke test** — registry, PDF helpers, PNG rendering, candidate finder, validation. ✅ All green.
2. **Overview generator** — real LLM call. Returned useful structure notes AND flagged:
   - Embedded old 2013/2019 pages as scan artifacts
   - Appendix 2 for outdoor staff (matches your ae526314 audit suspicion)
   - Part A/B/C structure
   - Operative period `2023-10-05 to 2026-06-30`
3. **Candidate finder** — initial keyword-only version missed the actual pay-table pages. **Fixed**: now prioritises pages with dollar-like decimals (>=3). Page 42 (the actual Appendix 5 rate table) now surfaces first.
4. **Vision extraction on page 42** — extracted 32 rows clean, Band 1A through 8D, weekly rates correctly identified. Table title, source clause ("Appendix 5"), effective_from ("2023-07-01") all captured. Band 2D correctly left null (that level doesn't exist — your MEMORY.md notes this is structurally normal).
5. **Save flow** — persists to `canonical/ae521669.yaml`, runs validation, returns validation list.
6. **Validation rules** — hit with synthetic bad data, all 6 rules fired:
   - `missing_effective_date` ✅
   - `monotonicity_break` ✅
   - `hourly_only` (ae526078 ref) ✅
   - `fortnightly_only` (ae526314 ref) ✅
   - `out_of_range_weekly` ✅
   - Restored real Pyrenees data after test.

## What's live right now in canonical/ae521669.yaml

- Overview generated by Sonnet 4.5 (with 4 red flags)
- 32-row pay table from p.42 (Part B Band 1A-8D, weekly, 2023-07-01)
- Status: `in_progress`

You can open the app, select Pyrenees Shire Council, go to Pay Tables, and see it populated.

## What needs attention

1. **Multi-table-per-extraction** — current extract returns only the first table on a page. Pyrenees has 4 or 5 rate tables across pages 42-44 (Part B for each uplift year, Part C, Appendix 2 outdoor). Workflow works (extract each page separately, append), but could be smarter.
2. **Browser test not run** — my browser tool blocks localhost by policy. You'll be the first to actually click around. I verified endpoints by curl; the frontend is vanilla HTML/JS so SHOULD render, but visual QA is on you.
3. **Edit UI** — extracted tables render as editable JSON textarea. Fine for MVP but clunky for long tables. Could be table-row editor later.
4. **2 PDFs on disk aren't in registry** — `ae512085` and `ae516921` show as AE IDs only (no council name). Minor — you'd see them in the dropdown with their IDs.
5. **Vision cost** — page-at-a-time extraction is pricey at ~$0.02 per page. Overview is ~$0.03. For 77 councils × 3-5 pay pages = ~$7 in vision calls. Budget-wise OK.

## Pain points REMOVED

- No more cut and paste. Click "Extract this page" and it does the work.
- No more blank prompt box. Playbook buttons drive the flow.
- Validation runs on save — you see anomalies immediately.
- Pipeline matrix shows all 77 councils × 5 sections at a glance.
- Overview surfaces structural red flags before you even open a table (e.g. "this one has outdoor staff in Appendix 2").

## Git

Committed to shared repo (one commit, only eba-workbench files). `canonical/*.yaml` is git-ignored — only the live Pyrenees file is present on disk for now; delete it if you want to start fresh.

## Suggested next moves

- Drive the app manually tomorrow: pick a different council (say ae516921 — unknown in registry, would test the edge case), generate overview, find candidates, extract.
- If the flow feels right, V3 adds: multi-table-per-page detection, uplift_rules playbook (second section), per-table effective-date reconciliation against the rules stream.
